#!/usr/bin/python3
# Return the number of keys in a dictionary


def number_keys(a_dictionary):
    """
    a_dictionary: is the dictionary with key, values
    """
    return len(a_dictionary.keys())
